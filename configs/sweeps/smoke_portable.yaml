base_config: ../attacks/schedule_skew.yaml
parameters:
  execution.model: [portable_qpv]
  simulation.rounds: [10]
  protocol.minimum_committed_rounds: [1]
  attacks[0].x_shift: ["-1 us", "0 us"]
  attacks[0].y_shift: ["-1 us", "0 us"]
replicates: 1
seed_strategy: deterministic_sequence
